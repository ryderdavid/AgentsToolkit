# Pull Request Protocol

Create draft PR immediately:

```bash
gh pr create --draft --title "[WIP] #{num}: {desc}" --body "Closes #{num}"
```

**Before marking ready:**
- All acceptance criteria checked
- Journey documented
- Commits squashed if noisy

See [PR Template](../../docs/AGENTS_REFERENCE.md#pr-template).
