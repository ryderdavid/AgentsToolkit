Create a GitHub issue with proper branch and screenshot handling.

Usage: Ask the user for title, body, and optional screenshot paths, then run:
`.agents/commands/issue.sh "title" "body" [screenshot.png]`


