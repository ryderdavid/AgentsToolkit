{
  "name": "agentstoolkit",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript agent registry and validation layer for AgentsToolkit",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "validate": "npm run build && node dist/cli/validate-agents.js"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
